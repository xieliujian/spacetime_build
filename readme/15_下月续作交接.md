# 下月续作交接

## 当前状态

- 总体设计已完成：`readme/00_全新构建系统设计.md`。
- 第一阶段工程骨架已完成且 Python 3.10+ 验证缺口已关闭：
  `readme/12_第一阶段实施计划.md`。
- 第二阶段纯 Python 领域内核已实现并通过自动化质量门禁：
  `readme/13_第二阶段领域模型与DAG实施计划.md`。
- 第三阶段兼容协议计划已完成，但协议代码和迁移验收尚未开始：
  `readme/14_第三阶段兼容协议实施计划.md`。
- 阶段 3 至阶段 14 的全系统路线图、设计和实施计划已写入 `readme/16` 至 `readme/31`，
  其中 17 至 31 号文档已完成独立审查；
  文档完成不表示阶段 3 以后代码或真实平台能力可用。
- 已建立根目录 `AGENTS.md` 和 `.agents/` 项目 Agent 资源。
- 原始目录 `F:\proj_se\develop\client\tools\build` 未修改。

## 已实现代码

- Python `src` 工程骨架；
- `st.build` 命名空间；
- Python、setuptools、pytest、Ruff 和 Pyright 配置；
- 包导入与版本测试；
- wheel 分发、元数据和隔离安装测试；
- Python 缓存及构建产物忽略规则。
- 完整业务异常体系、不可变产物模型和 BuildExecutionRecord；
- 确定性 BuildManifest 工厂、编解码和完整性校验；
- BuildTask/TaskPlan/TaskIdentity、BuildGraph 和 BuildPlanner；
- 可校验恢复 Frontier 和确定性同步参考 Executor；
- ReleaseEntry、ReleaseSnapshot、ReleaseManifest 和 ReleaseBundle；
- ReleaseActivationRecord、远端对象验证证明和合法状态迁移。

尚未实现：

- 六字段文件列表和 `assetbundledb_*.txt` writer/parser；
- Redirect、分包和 CDN 旧客户端协议输出；
- SVN、Unity、Jenkins 和 CDN 适配器；
- 真实资源构建任务、上传、CAS 激活、回滚和 CLI。
- Android、iOS、Windows 客户端打包、IL2CPP/SDK、分支和美术辅助能力。

## 当前验证基线

2026-08-07 在 Python 3.10.11 环境验证：

- `python -m pytest -q`：`51 passed`，无 skip；
- 整体覆盖率：93%；
- `st.build.core` 覆盖率：93%；
- `st.build.release` 覆盖率：93%；
- Ruff format、Ruff check、Pyright 和 compileall：全部通过。

## 下次会话第一步

从第三阶段兼容协议的第一个 TDD 切片开始前，重新确认当前基线：

```powershell
python --version
python -m pip install -e ".[dev]"
python -m pytest -q
python -m ruff check .
python -m pyright
python -m compileall -q src tests
```

Python 版本必须为 3.10 或更高，完整测试不得出现 skip。

## 后续执行顺序

1. 完成第二阶段中文 docstring 详细度人工审查并记录结论；
2. 按 `readme/14_第三阶段兼容协议实施计划.md` 实现版本化换行策略；
3. 实现六字段文件列表 DTO、writer、parser 和合成 Golden；
4. 实现 AssetBundle 数据库 DTO、五库路由、writer、parser 和合成 Golden；
5. 从旧系统隔离副本捕获 historical fixture，执行七文件字节级 diff；
6. 使用旧客户端 parser 关闭迁移验收；
7. 按 `readme/18_外部集成适配器设计与实施计划.md` 实现外部端口与适配器；
8. 按 19 至 21 号文档实现资源、发布、缓存和恢复；
9. 按 26 号文档实现唯一 application/CLI 编排层；
10. 按 22 至 25、27、28 号文档逐平台实现客户端包体和扩展；
11. 按 29、30 号文档实现分支与美术辅助组件；
12. 最后按 31 号文档逐能力完成隔离双跑、灰度和回滚验收。

## 尚未关闭的风险

- 第二阶段中文 docstring 详细度人工审查尚未留下完整记录；
- 旧客户端真实解析器尚未接入；
- historical 五个 AB 数据库及 main/low 文件列表 fixture 尚未捕获；
- 旧系统与新 writer 的字节级双跑尚未执行；
- 当前无 CLI、外部适配器或真实资源任务，不能执行实际构建和发布；
- 第三阶段完成代码不等于迁移兼容验收完成。
- 后续设计与实施计划虽已齐全，但所有未实现模块仍是规划中，不能把文档命令当作当前命令运行。

## 恢复提示

新会话可直接要求：

> 读取 AGENTS.md、readme/00_全新构建系统设计.md、readme/14_第三阶段兼容协议实施计划.md、readme/17_全系统实施总路线图.md 和 readme/15_下月续作交接.md，从第三阶段 Task 1 开始继续实现。

继续工作时不得修改或直接运行原始 `F:` 目录中可能产生写入、提交或上传副作用的入口。
