# 下月续作交接

## 当前状态

- 总体设计已完成：`readme/00_全新构建系统设计.md`。
- 工程骨架已完成：`readme/12_第一阶段实施计划.md`。
- 第二阶段计划已完成：`readme/13_第二阶段领域模型与DAG实施计划.md`。
- 第三阶段计划已完成：`readme/14_第三阶段兼容协议实施计划.md`。
- 已建立根目录 `AGENTS.md` 和 `.agents/` 项目 Agent 资源。
- 原始目录 `F:\proj_se\develop\client\tools\build` 未修改。
- 未创建 Git commit。

## 已实现代码

- Python `src` 工程骨架；
- `st.build` 命名空间；
- Python、setuptools、pytest、Ruff 和 Pyright 配置；
- 包导入与版本测试；
- wheel 分发、元数据和隔离安装测试；
- Python 缓存及构建产物忽略规则。

当前环境为 Python 3.7.8，因此 wheel 分发测试被明确跳过；其余结果为 `2 passed, 1 skipped`。

## 下次会话第一步

先准备 Python 3.10+ 环境，然后执行：

```powershell
python --version
python -m pip install -e ".[dev]"
python -m pytest -q
python -m ruff check .
python -m pyright
python -m compileall -q src tests
```

Python 版本检查必须返回 3.10 或更高。分发测试必须实际执行，不能继续保持 skip。

## 后续执行顺序

1. 关闭第一阶段 Python 3.10+ 验证缺口；
2. 按 `readme/13_第二阶段领域模型与DAG实施计划.md` 实现中文 docstring 门禁；
3. 实现异常、Artifact、Manifest、任务身份和 DAG；
4. 实现安全恢复 Frontier 和 Executor 输出契约；
5. 实现 ReleaseSnapshot、ReleaseManifest、ReleaseBundle 和激活验证证明；
6. 完成第二阶段规格审查和代码质量审查；
7. 按 `readme/14_第三阶段兼容协议实施计划.md` 实现旧协议。

## 尚未关闭的风险

- Python 3.10+ 尚未在当前机器验证；
- 旧客户端真实解析器尚未接入；
- historical 五个 AB 数据库及 main/low 文件列表 fixture 尚未捕获；
- 旧系统与新 writer 的字节级双跑尚未执行；
- 第三阶段完成代码不等于迁移兼容验收完成。

## 恢复提示

新会话可直接要求：

> 读取 AGENTS.md、readme/00_全新构建系统设计.md 和 readme/15_下月续作交接.md，从 Python 3.10+ 验证开始继续实现。

继续工作时不得修改或直接运行原始 `F:` 目录中可能产生写入、提交或上传副作用的入口。
