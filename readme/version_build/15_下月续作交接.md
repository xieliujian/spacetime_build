# 下月续作交接

## 当前状态

- 总体设计已完成：`readme/version_build/00_全新构建系统设计.md`。
- 第一阶段工程骨架已完成且 Python 3.10+ 验证缺口已关闭：
  `readme/version_build/12_第一阶段实施计划.md`。
- 第二阶段纯 Python 领域内核已实现并通过自动化质量门禁：
  `readme/version_build/13_第二阶段领域模型与DAG实施计划.md`。
- 第三阶段兼容协议代码与合成 Golden 已完成，自动化目标门禁通过，但真实迁移验收仍未关闭：
  `readme/version_build/14_第三阶段兼容协议实施计划.md`。
- 正式版本制作第二层外部集成适配器已完成自动化实现，第三层资源公共模型和自动化任务契约
  已完成，记录和后续入口见：
  `readme/release_build/02_第二层外部集成适配器实施计划.md`。
- 阶段 3 至阶段 14 的全系统路线图、设计和实施计划已写入 `readme/16` 至 `readme/31`，
  其中 17 至 31 号文档已完成独立审查；
  文档完成不表示阶段 3 以后代码或真实平台能力可用。
- 已建立根目录 `AGENTS.md` 和 `.agents/` 项目 Agent 资源。
- 原始目录 `F:\proj_se\develop\client\tools\build` 未修改。

## 已实现代码

- Python `src` 工程骨架；
- `core` 和 `release` 直接顶级包；
- Python、setuptools、pytest、Ruff 和 Pyright 配置；
- 包导入与版本测试；
- wheel 分发、元数据和隔离安装测试；
- Python 缓存及构建产物忽略规则。
- 完整业务异常体系、不可变产物模型和 BuildExecutionRecord；
- 确定性 BuildManifest 工厂、编解码和完整性校验；
- BuildTask/TaskPlan/TaskIdentity、BuildGraph 和 BuildPlanner；
- 可校验恢复 Frontier 和确定性同步参考 Executor；
- ReleaseEntry、ReleaseSnapshot、ReleaseManifest 和 ReleaseBundle；
- ReleaseActivationRecord、远端对象验证证明和合法状态迁移。
- `compatibility` 六字段文件列表与 AssetBundle 五库 DTO、Writer、Parser 及合成 Golden。

尚未实现：

- 真实历史六字段文件列表和 `assetbundledb_*.txt` writer/parser 双跑证据；
- Redirect、分包和 CDN 旧客户端协议输出；
- 真实 SVN、Unity、Jenkins、Secrets 和供应商 CDN 平台探针；
- 真实 Unity 资源构建规则、上传、CAS 激活、回滚和 CLI。
- Android、iOS、Windows 客户端打包、IL2CPP/SDK、分支和美术辅助能力。

## 当前验证基线

2026-08-20 在 Python 3.10.11 环境验证：

- compatibility/core/release/resource 目标回归：`470 passed`（排除已知长时间无输出的进程树节点）；
- resource 独立覆盖率：86%；compatibility/core/release 原有覆盖率基线保持不变；
- Ruff format、Ruff check、Pyright 和 compileall：全部通过；
- `tests/integrations/test_process.py` 的进程树长时节点仍未形成稳定完整回归证据。

## 下次会话第一步

从资源构建公共模型的第一个 TDD 切片开始前，重新确认当前基线；兼容协议代码可直接被
后续 release/application 组合层消费：

```powershell
python --version
python -m pip install -e ".[dev]"
python -m pytest -q
python -m ruff check .
python -m pyright
python -m compileall -q src tests
```

Python 版本必须为 3.10 或更高，完整测试不得出现 skip。

## 后续执行顺序

1. 继续按 `readme/release_build/00_正式版本资源构建与发布设计.md` 补齐任务工具适配、结果包持久化
   与真实输入 fixture；旧 `version_build/19/20` 仅作历史参考；
3. 实现精确输出发现、产物校验、确定性缓存和单任务执行服务；
4. 再实现十二个具体资源任务；
5. 从旧系统隔离副本捕获 historical fixture，执行字节级 diff 和旧客户端解析验证；
7. 按 21 号文档实现发布缓存、恢复、上传和 CAS 激活；
8. 按 26 号文档实现唯一 application/CLI 编排层；
10. 按 22 至 25、27、28 号文档逐平台实现客户端包体和扩展；
11. 按 29、30 号文档实现分支与美术辅助组件；
12. 最后按 31 号文档逐能力完成隔离双跑、灰度和回滚验收。

## 尚未关闭的风险

- 第二阶段中文 docstring 详细度人工审查尚未留下完整记录；
- 旧客户端真实解析器尚未接入；
- historical 五个 AB 数据库及 main/low 文件列表 fixture 尚未捕获；
- 旧系统与新 writer 的字节级双跑尚未执行；
- 当前无 CLI、真实 Unity 资源打包或发布编排，不能执行实际构建和发布；资源任务公共层仅完成
  自动化契约和文件型 fixture；第二层适配器仅完成自动化边界，
  真实平台仍未验收；
- 第三阶段完成代码不等于迁移兼容验收完成。
- 后续设计与实施计划虽已齐全，但所有未实现模块仍是规划中，不能把文档命令当作当前命令运行。

## 恢复提示

新会话可直接要求：

> 读取 AGENTS.md、readme/release_build/02_第二层外部集成适配器实施计划.md、
> readme/release_build/00_正式版本资源构建与发布设计.md，继续补齐真实工具适配、配置/结果交接
> 和发布前置模型；旧 readme/version_build/19/20 仅作历史参考。

继续工作时不得修改或直接运行原始 `F:` 目录中可能产生写入、提交或上传副作用的入口。
