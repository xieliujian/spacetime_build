# 第一阶段工程骨架记录 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 记录已经完成的 Python 3.10+ 直接顶级包工程骨架、TDD 证据、审查结果和尚未关闭的环境验证缺口。

**Architecture:** 本阶段只建立 setuptools `src` 布局、公共导入入口、版本标识和基础质量工具配置。领域模型、任务 DAG、发布模型和旧客户端兼容协议不属于本阶段，分别移入后续独立计划。

**Tech Stack:** Python 3.10+、setuptools、pytest、pytest-cov、Ruff、Pyright。

**设计文档:** `readme/version_build/00_全新构建系统设计.md`

---

## 阶段状态

状态：**已完成，Python 3.10+ 环境验证缺口已关闭**。

- [x] 工程骨架实现完成。
- [x] Task 1 规格审查通过。
- [x] Task 1 代码质量审查通过。
- [x] 历史 Python 3.7.8 环境中的可执行回归为 `2 passed, 1 skipped`。
- [x] 已在 Python 3.10.11 环境执行 wheel 构建、元数据、隔离安装和隔离导入测试。

“已完成”只表示本阶段源码和配置已达到审查要求，不表示后续领域、DAG、Release 或 compatibility 能力可用。

## 已完成文件与职责

- `pyproject.toml`：声明 Python `>=3.10`、setuptools `src` 包发现、静态发行版本、pytest、Ruff、Pyright strict 和开发依赖。
- `.gitignore`：忽略缓存和根级构建产物，同时避免误忽略 `src/`。
- `src/core/__init__.py`：提供核心领域顶级包，不产生外部副作用。
- `src/release/__init__.py`：提供发布领域顶级包，不产生外部副作用。
- `tests/test_package_import.py`：验证两个顶级包从源码目录直接导入。
- `tests/test_distribution.py`：验证源码不被 Git 忽略；在 Python 3.10+ 构建 wheel 并检查元数据、包内容、隔离安装和隔离导入。
- `README.md`：只描述工程骨架和仍处于规划中的能力。

## Task 1：建立 Python 工程与质量工具

**Files:**

- Create: `pyproject.toml`
- Create: `.gitignore`
- Create: `src/core/__init__.py`
- Create: `src/release/__init__.py`
- Create: `tests/test_package_import.py`
- Create: `tests/test_distribution.py`
- Modify: `README.md`

- [x] **Step 1：先写包导入失败测试**

目标测试：`tests/test_package_import.py::test_top_level_packages_are_directly_importable`。

目标 API：`core` 和 `release` 顶级包。

断言：两个包均从仓库 `src/` 下的对应目录导入。

- [x] **Step 2：执行 RED**

Run: `python -m pytest tests/test_package_import.py -q`

记录结果：迁移测试先确认 wheel 仍包含旧包目录而失败；原始命令日志未保存到仓库，不把该证据描述为可从当前工作区重新生成的 RED。

- [x] **Step 3：实现最小 GREEN**

最小实现包括：

- setuptools `src` package discovery；
- pytest `pythonpath = ["src"]`；
- `src/core/__init__.py`；
- `src/release/__init__.py`；
- `pyproject.toml` 静态发行版本 `0.1.0`；
- Python `>=3.10`、Ruff、Pyright strict 和 dev 依赖配置；
- 与当前能力一致的根 README。

- [x] **Step 4：执行 GREEN**

Run: `python -m pytest tests/test_package_import.py -q`

记录结果：`1 passed in 0.01s`。

- [x] **Step 5：按质量审查补充工程分发回归**

新增行为：

- `tests/test_distribution.py::test_source_packages_are_not_ignored` 使用 `git check-ignore --no-index` 验证两个源码包不受根级或全局忽略规则误伤；
- `tests/test_distribution.py::test_wheel_metadata_content_and_isolated_import` 在 pytest 临时目录复制最小工程，构建 wheel，检查 `core/__init__.py`、`release/__init__.py`、发行名、版本和 `Requires-Python`，再隔离安装和导入；
- wheel 测试在 Python 3.10 以下通过 `pytest.mark.skipif` 明确跳过，不把 Python 3.7 的有限验证误称为目标环境验收。

- [x] **Step 6：执行当前环境最终回归**

Run: `python -m pytest tests/test_package_import.py tests/test_distribution.py -q`

已记录结果（Python 3.7.8）：`2 passed, 1 skipped`。

其中：

- 包导入测试通过；
- Git 可见性测试通过；
- wheel 分发契约测试因解释器低于 Python 3.10 而跳过。

同时已记录：

- `python -m compileall -q src tests`：exit code 0；
- `git diff --check`：exit code 0；
- 未修改 `F:\proj_se\develop\client\tools\build`；
- 未创建 Git commit；
- 规格审查结论：`SPEC COMPLIANT`；
- 质量审查完成后没有遗留 Task 1 代码问题。

- [x] **Step 7：在受支持解释器关闭 wheel 验证缺口**

前置条件：获得 Python 3.10+，并在该解释器的隔离虚拟环境安装 `.[dev]`。

Run:

```text
python --version
python -m pip install -e ".[dev]"
python -m pytest tests/test_package_import.py tests/test_distribution.py -q
python -m ruff format --check .
python -m ruff check .
python -m pyright
python -m compileall -q src tests
```

Expected:

- `python --version` 为 3.10 或更高；
- pytest 为 `3 passed`，不得出现 wheel 测试 skip；
- wheel 中包含 `core/__init__.py` 和 `release/__init__.py`，且不包含旧包目录；
- METADATA 的 Name、Version、Requires-Python 分别为 `spacetime-build`、`0.1.0`、`>=3.10`；
- 隔离安装后从临时安装目录导入 `core` 和 `release`；
- Ruff、Pyright、compileall 全部 exit code 0。

若仍只有 Python 3.7，必须停止功能阶段实现，不得通过取消 skip、降低 `requires-python` 或把 Python 3.7 结果当作 Python 3.10 验收来绕过门禁。

2026-08-07 复验记录：

- `python --version`：`Python 3.10.11`；
- 完整回归：`51 passed`，无 skip，包含 wheel 构建、元数据、隔离安装和隔离导入测试；
- `python -m ruff format --check .`、`python -m ruff check .`、`python -m pyright`
  和 `python -m compileall -q src tests`：全部 exit code 0。

## 本阶段明确不包含

- 领域异常和产物模型；
- BuildManifest、BuildExecutionRecord 和确定性 manifest；
- 任务协议、BuildGraph、Planner、Executor 和恢复 Frontier；
- ReleaseManifest、ReleaseBundle 和 ReleaseActivationRecord；
- 六字段文件列表、AssetBundle 数据库和其他旧客户端协议；
- 资源构建、外部适配器、上传、激活与回滚。

上述工作分别见：

- `readme/version_build/13_第二阶段领域模型与DAG实施计划.md`；
- `readme/version_build/14_第三阶段兼容协议实施计划.md`。

## 提交说明

本记录不授权创建 Git commit。只有用户明确要求时才提交。
