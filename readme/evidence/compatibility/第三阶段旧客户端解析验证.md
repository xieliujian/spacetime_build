# 第三阶段旧客户端解析验证

状态：PENDING

## 当前边界

第三阶段兼容协议的自动化代码、合成 Golden 和新 Parser 可在本仓库内验证。真实历史
五库、main/low 文件列表、旧系统隔离副本输出和旧客户端 Parser 尚未采集，因此本
证据不能标记为 `VERIFIED`，也不能用合成 fixture 替代迁移验收。

## 待补证据

- 旧客户端版本或 commit：待采集。
- 隔离工作区和输入快照 SHA256：待采集。
- 五个 `assetbundledb_*.txt` 历史 bytes 与 SHA256：待采集。
- main/low 六字段文件列表历史 bytes 与 SHA256：待采集。
- 旧 writer 与新 writer 的逐文件 bytes diff：待执行。
- 旧客户端 Parser 对历史和合成边界 fixture 的逐文件结果：待执行。
- 执行命令、退出码、stdout/stderr 摘要和验证人：待执行。

## 自动化状态

本阶段只使用 `tests/fixtures/compatibility/synthetic/` 下的独立合成 bytes，覆盖 LF、
CRLF、历史 URL、重复依赖和 Redirect。上述测试通过不代表真实旧客户端迁移验收通过。
